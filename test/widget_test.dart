import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Akeli app smoke test', (WidgetTester tester) async {
    // Placeholder — real tests require Supabase init
    expect(true, isTrue);
  });
}
