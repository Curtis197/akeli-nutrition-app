# Agent Behavior Rules

- After completing any plan given by the user, the agent MUST create a proper walkthrough document inside the project repository. The document should be placed in `docs/superpowers/walkthroughs/` and named descriptively based on the plan's contents (e.g. `YYYY-MM-DD-plan-name.md`).
